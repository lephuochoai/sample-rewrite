import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    assetPrefix: 'https://sample-rewrite-rocvzzg7i-phuoc-hoais-projects.vercel.app',  
};

export default nextConfig;
